// Mach-O __TEXT sections that carry executable symbols in linker map files.
// Included from SectionNames.h with MACHO_EXECUTABLE_MAP_SECTION(SectionName).
// StringSwitch::Case requires string literals; values must match
// section_names::macho::{Text,Stubs}.

MACHO_EXECUTABLE_MAP_SECTION("__text")
MACHO_EXECUTABLE_MAP_SECTION("__stubs")
