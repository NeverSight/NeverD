cmake_minimum_required(VERSION 3.24)
project(NeverDGuiBenchmarks LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_AUTOMOC ON)
find_package(Qt6 6.5 REQUIRED COMPONENTS Core Gui Qml Quick)
qt_add_executable(neverd-viewport-bench viewport_bench.cpp)
target_link_libraries(neverd-viewport-bench PRIVATE Qt6::Core Qt6::Gui Qt6::Qml Qt6::Quick)
target_compile_definitions(neverd-viewport-bench PRIVATE BENCH_BUILD_TYPE="${CMAKE_BUILD_TYPE}")
if(WIN32)
  target_link_libraries(neverd-viewport-bench PRIVATE psapi)
endif()
