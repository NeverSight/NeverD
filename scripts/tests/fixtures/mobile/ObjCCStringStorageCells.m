// A separate translation unit keeps actual loads of the readonly pointer slots.
const char *const NDStoredCStringLabel = "persist-queue-label";
const char *const NDStoredCStringSuffix = "persist-queue-label" + 8;
