package fixture;

// A legal class name that also names this package. Qualified generated type
// names must not accidentally resolve through this class.
public class fixture {
    public static AndroidPeer identity(AndroidPeer value) { return value; }
    public static int twice(int value) { return AndroidPeer.twice(value); }
}
